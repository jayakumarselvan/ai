# Required: your OpenAI key, used by LiteLLM under the hood
OPENAI_API_KEY=sk-...

# Optional: override the LiteLLM model string (defaults to openai/gpt-4o-mini)
# TODO_LLM_MODEL=openai/gpt-4o-mini

# Optional: override where the JSON "database" file lives
TODO_DATA_DIR=./data
